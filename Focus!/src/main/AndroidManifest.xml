<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pk.example"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".NLService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- <service -->
        <!-- android:name=".ProfileJobService" -->
        <!-- android:label="ProfileJobService" -->
        <!-- android:permission="android.permission.BIND_JOB_SERVICE" > -->


        <!-- </service> -->

        <activity
            android:name=".ProfileViewActivity"
            android:label="@string/title_activity_create_profile"
            android:parentActivityName=".ProfileListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pk.example.ProfileListActivity" />
        </activity>
        <!-- <activity -->
        <!-- android:name=".TabbedActivity" -->
        <!-- android:label="@string/title_activity_tabbed" -->
        <!-- android:parentActivityName=".MainActivity"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value="com.pk.example.MainActivity" /> -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".ScheduleListActivity" -->
        <!-- android:label="@string/title_activity_schedule_list" -->
        <!-- android:parentActivityName=".MainActivity"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value="com.pk.example.MainActivity" /> -->
        <!-- </activity> -->
        <activity
            android:name=".ScheduleListActivity"
            android:label="@string/title_activity_schedule_list"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pk.example.MainActivity" />
        </activity>
        <activity
            android:name=".ProfileListActivity"
            android:label="View Profiles"
            android:parentActivityName=".MainActivity">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="com.pk.example.MainActivity" />
        </activity>
        <activity
            android:name=".ScheduleViewActivity"
            android:label="Schedule"
            android:parentActivityName=".ScheduleListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pk.example.ScheduleListActivity" />
        </activity>
        <receiver android:name=".NotificationReceiver" >
            <intent-filter>
                <action android:name="com.pk.example.INSERT_NOTIFICATION"/>
                <action android:name="com.pk.example.ADD_PROFILE"/>
                <action android:name="com.pk.example.REMOVE_PROFILE" />
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </receiver>
        <activity
            android:label="Notification List"
            android:name=".NotificationListActivity"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pk.example.ScheduleListActivity"/>
        </activity>
    </application>

</manifest>